import { GridAutosizeOptions } from "@mui/x-data-grid";

export const DEFAULT_PAGE_SIZE_OPTIONS = [5, 10, 25, 50, 100];
export const DEFAULT_AUTOSIZE_OPTIONS: GridAutosizeOptions = {
    expand: true,
    includeHeaders: true,
    includeOutliers: true,
};
export const DEFAULT_CHECKBOX_WIDTH = 50;
