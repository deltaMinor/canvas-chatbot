export type WizardStep = 1 | 2;

export enum WizardStepEnum {
    one = 1,
    two = 2,
}

export enum CQFormContentView {
    startupWizard = "startupWizard",
    formStack = "formStack",
    summary = "summary",
}
